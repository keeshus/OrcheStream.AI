🎉 Core Agents has been deployed!

{{- if .Values.ingress.enabled }}
Access the app at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
Access the frontend locally:
  kubectl port-forward svc/{{ include "orchestream-ai.fullname" . }}-frontend {{ .Values.frontend.port }}:{{ .Values.frontend.port }}

Then open http://localhost:{{ .Values.frontend.port }}
{{- end }}

Backend health check:
  kubectl exec deploy/{{ include "orchestream-ai.fullname" . }}-backend -- curl -s http://localhost:{{ .Values.backend.port }}/api/health

Database migrations run automatically via an init container on the backend pod.

{{- if not .Values.postgres.enabled }}
⚠️  PostgreSQL is disabled. Set postgres.enabled=true or provide an external databaseUrl.
{{- end }}

{{- if and .Values.postgres.enabled (not .Values.existingSecret) }}
⚠️  dbPassword was provided at install time and is stored in the chart Secret.
    Prefer the existingSecret pattern for production credentials.
{{- end }}

{{- if and (not .Values.anthropicApiKey) (not .Values.existingSecret) }}
⚠️  No Anthropic API key set. LLM endpoints using Anthropic will fail until configured.
{{- end }}
